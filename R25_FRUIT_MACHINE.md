# R25 欢乐水果机

## 范围

- 保留技术 ID `merge2048`、路由 `/games/merge2048` 和排行榜兼容性。
- 运行时完整替换为 24 格环形跑灯水果机，展示名为“欢乐水果机”。
- 八门顺序固定为：BAR、77、星星、西瓜、铃铛、芒果、橙子、苹果。
- 首版包含普通大/小图标、送灯、小玛丽固定 3 次和铜/银/金三档彩金。
- 猜大小、大小三元、自动连开不在 R25 范围。

## 服务端结算

接口：`POST /api/v1/user/game-wallet/fruit/spin`

请求必须携带 `Idempotency-Key`，下注按字符串传输：

```json
{
  "bets": {
    "bar": "1",
    "77": "0",
    "star": "0",
    "watermelon": "0",
    "bell": "0",
    "mango": "0",
    "orange": "0",
    "apple": "2"
  }
}
```

响应核心字段：

```json
{
  "stop_index": 7,
  "outcome": {
    "kind": "fruit",
    "symbol": "watermelon",
    "size": "small",
    "multiplier": "3"
  },
  "payout": "6",
  "credits_after": "994"
}
```

`outcome.kind` 可为 `fruit`、`send_light`、`little_mary`、`jackpot`。送灯的全部额外停格、小玛丽 3 次停格以及彩金档位均由一次服务端请求生成并结算，客户端不预扣、不补算。

单门下注上限 100，总注上限 800。扣注、派奖、不可变账本和审计记录在同一数据库事务中完成。流水类型为 `fruit_bet`、`fruit_payout`，审计表为 `game_fruit_spins`。

## 权重和 RTP

固定虚拟权重总数为 10,000：

- 16 个普通格合计 9,000。
- 3 个送灯格合计 800；额外格数量 1/2/3 的权重为 60/30/10。
- 2 个小玛丽格合计 150；固定生成 3 个普通水果停格。
- 铜/银/金彩金权重分别为 30/15/5，按总注支付 8/25/80 倍。
- 大图标倍率：100/40/30/20/20/15/10/5；小图标均为 3 倍。

精确理论 RTP 由单元测试使用有理数计算：

- BAR、星星、西瓜、铃铛、芒果、橙子、苹果：约 `92.65%`。
- 77 因整数权重取整：约 `92.89%`。
- 任意多门组合是各单门的下注加权平均，因此仍位于 `92.65%–92.89%`，并且每门均不超过 100%。

## 兼容与风险

- 本地最高分和本地日榜键增加 `fruit-v1` 后缀，不读取旧 2048 本地分数。
- 服务端仍使用 `game_id=merge2048`。部署当天若已有旧 2048 日榜成绩，会混榜到本地日期结束；建议在日切后部署，或上线前单独决定是否清理当天旧榜。
- 旧 `Merge2048Scene` 和规则文件保留为未引用回滚材料，生产场景工厂已只加载 `FruitMachineScene`。
- 数据库迁移 `195_game_fruit_machine.sql` 必须先成功执行，旧二进制不认识新增流水类型但不受新增表影响。
