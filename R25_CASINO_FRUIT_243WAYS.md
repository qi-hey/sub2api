# Sub2API 0.1.165-r25 游戏中心更新

## 更新范围

R25 保留 R11-R24 的全部二开功能，只更新游戏中心：

- 金运老虎机从 20 条固定线改为 5 轴 3 行、243 Ways。
- BONUS 既可替代普通图案，又会让参与组合的奖值逐个翻倍。
- 前 3 轴各使用 60 个固定停格，福利局触发率为 `1.5625%`。
- 普通中奖、大奖和头奖使用不同的原创合成音效与金币动画。
- 欢乐水果机完整替换旧 2048 运行入口，技术 ID 仍为 `merge2048`。
- 水果机使用 24 格环形跑灯、8 门独立下注、送灯、小玛丽和三档彩金。
- 所有可兑换积分均由服务端 RNG、事务和不可变账本权威结算。

## 概率与返还

老虎机：

- 基础转动 RTP（含普通组合与 SCATTER，不含奖励局和免费转）：约 `70.2286%`。
- 奖励局贡献估算：约 `18.23%`。
- 每次转动平均赠送免费转 `0.00837056` 次；包含递归赠送后的倍率为 `1.00844121`。
- 含奖励局和免费转的总理论 RTP 估算约 `89.2%`。

水果机：

- 单门理论 RTP 约 `92.65%-92.89%`。
- 单门下注上限 100，总注上限 800。
- 详细权重、倍率和接口见 `R25_FRUIT_MACHINE.md`。

## 安全与一致性

- 水果机请求必须携带幂等键，网络结果不确定时只能按原下注重放。
- 暂停状态禁止下注和启动，避免动画暂停后仍发生服务端扣分。
- 下注门名归一化后不允许重复键。
- 已落库的幂等结果在 RNG 前读取，重放不依赖新随机数。
- BONUS 未领取奖励必须可在刷新或重新进入后恢复，过期或已领取奖励不得恢复。
- 迁移 195 只能替换目标流水类型约束，不得删除其他组合约束。

## 部署步骤

1. 上传源码到 `/opt/sub2api/builds/0.1.165-r25/`。
2. 在 VPS 上运行定向测试、迁移测试和 `go test ./...`。
3. 构建 `-tags embed` 二进制，版本 `0.1.165-r25`、提交标识 `custom-r25`。
4. 使用 `pg_dump` 备份 PostgreSQL，并备份 R24 二进制。
5. 原子替换 `/opt/sub2api/sub2api`，仅执行一次服务重启。
6. 验证版本、服务状态、重启次数、网页、游戏接口和 OpenAI API。

## 回滚

- 若迁移或启动失败，立即恢复 R24 二进制并重启服务。
- 迁移 195 仅新增水果机表和扩展流水类型；旧二进制不读取新表。
- 如需完整数据回滚，使用部署前 PostgreSQL 备份恢复。

## 部署记录

- 目标版本：`0.1.165-r25`（`custom-r25`）。
- 生产部署：`2026-07-29 06:40:03 UTC`（北京时间 `2026-07-29 14:40:03`）。
- VPS 构建目录：`/opt/sub2api/builds/0.1.165-r25`。
- 在线二进制：`/opt/sub2api/sub2api`，SHA-256 `25843d5b0a1a86a4227da18399be8b6efe9ef1bd70b0348cb2b6936df1c43b7f`。
- 部署前备份目录：`/opt/sub2api/backups/r25-deploy-20260729T063649Z`。
- 数据库备份 SHA-256：`5f22ca828ef1b856e34f8b631689e4b3fa39cd489e59f2a10cd3b449ee0cc701`；`pg_restore --list` 校验通过。
- R24 二进制备份 SHA-256：`28a30ea527fd2c50e5ecaf03dcea73318f239f26725c3cc93f0ae2cd068e550a`。
- 迁移 195 已登记，数据库 checksum：`1bf2e70c0eab568fc45336faef6218335ccc14711088af529fe355e439fa7110`。
- 最终源码包：`/opt/sub2api/builds/0.1.165-r25/sub2api-0.1.165-r25-source.tar.gz`；SHA-256 记录在同目录的 `.sha256` 文件中。

## 上线验证

- `sub2api.service` 为 `active/running`，部署后重启次数为 `0`。
- 本地 `/health`、本地 `/games` 和公网 `https://sub.hey77.uk/games` 均返回 HTTP 200。
- 部署后真实 `gpt-5.6-sol /v1/responses` 请求返回 HTTP 200。
- 桌面 `1440x900` 与手机 `390x844` 已验收 `/games`、`/games/lucky`、`/games/merge2048`。
- 两个游戏画布像素非空，老虎机跑灯持续变化；暂停和恢复均正常。
- 页面无文档级横向溢出，中文文案、243 Ways 奖表、手机触控区和水果机八门下注区均正确显示。
- 浏览器控制台未发现来自 `sub.hey77.uk` 的页面错误。
