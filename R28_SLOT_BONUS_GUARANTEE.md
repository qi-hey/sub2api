# Sub2API 0.1.165-r28 免费旋转奖励局保障

R28 保留此前 R11-R27 的二开功能，只调整金运老虎机的 BONUS 免费旋转奖励局。

## 生效范围

- 只影响新的 `slot-bonus-v4` 免费旋转奖励局。
- 普通付费转动、SCATTER 免费转动、BONUS 触发概率 `109 / 8000 = 1.3625%` 和已修正的彩金倍率规则均不变。
- 已经生成并等待领取的 `slot-bonus-v3` 奖励局继续按原始结果领取，不会失效或被重算。

## 奖励局保障

- `8 轮 x5` 至少命中 3 轮。
- `12 轮 x3` 至少命中 5 轮。
- `20 轮 x2` 至少命中 8 轮。
- 三档命中下限约为 40%，用于避免整局全空。
- 整局最终奖励保底为 `下注 x 5 x 触发彩金倍率`。
- 3、4、5 个 BONUS 的触发彩金倍率仍为 `x1 / x2 / x4`。

为满足命中下限而替换的空转使用服务端固定、可由当前转轴复现的非触发元宝三轴结果；所有结果在用户选择方案之前生成并持久化，因此刷新、重试或并发领取不能改变结果。

## 前端节奏

- 免费旋转每轴停留从约 65ms 调整到约 170ms。
- 每次结果展示从约 360ms 调整到约 750ms。
- 规则面板增加免费旋转的命中下限和整局保底说明。

## 兼容性与验证

- 后端同时接受并校验 `slot-bonus-v3` 与 `slot-bonus-v4`。
- 无需数据库迁移。
- 增加固定保底盘面、命中下限、奖励保底和 V3/V4 JSON 兼容测试。
