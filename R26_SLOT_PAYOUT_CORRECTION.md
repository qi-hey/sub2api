# Sub2API 0.1.165-r26 老虎机派奖修正

## 修正内容

R26 保留 R11-R25 的全部二开功能，只修正金运老虎机的服务端计奖和前端奖表：

- `BONUS` 不再替代任何普通图案，也不参与 243 Ways 连轴计奖或倍数计算。
- `WILD` 仍可替代普通图案，但不单独赔付。
- `BONUS` 保留三个以上触发奖励游戏的行为，停格触发率仍为 `1.5625%`。
- 服务端支付表版本升级为 `slot-paytable-v4`。
- 前后端共用的新普通图案奖倍如下：

| 图案 | 3 轴 | 4 轴 | 5 轴 |
| --- | ---: | ---: | ---: |
| 金色 7 | x96 | x360 | x1440 |
| 龙 | x48 | x192 | x600 |
| 元宝 | x12 | x72 | x288 |
| 玉牌 | x6 | x48 | x192 |
| 金铃 | x6 | x12 | x120 |

奖倍按每组奖值计算，实际派奖仍为 `floor(中奖组合总奖倍 × 总下注 / 243)`。

## 概率与返还

- 枚举前 3 轴全部停格与后 2 轴基础组合，共核验 `437,400,000` 个组合。
- 移除 `BONUS` 百搭后，基础转动 RTP 为 `62.9733%`。
- 奖励游戏贡献保持约 `18.23%`，总理论返还约 `82%`，低于 R25。
- 规则变更不影响既有积分余额、历史流水、未领取奖励局和水果机。

## 回归保护

- 固定复现用户截图中的元宝与 `BONUS` 盘面：修正后只计算两条真实元宝组合，每条严格使用奖表 `x288`。
- 覆盖 `BONUS` 不替代、纯 `BONUS` 不赔付、`WILD` 替代、SCATTER、免费旋转和 RTP 确定性测试。
- 前端规则面板测试要求显示新奖倍，并明确显示“BONUS 不作百搭、不参与中奖倍数”。

## 部署与回滚

- 目标版本：`0.1.165-r26`，构建标识：`custom-r26`。
- VPS 构建目录：`/opt/sub2api/builds/0.1.165-r26`。
- 部署前备份在线 R25 二进制和 PostgreSQL 数据库。
- 原子替换 `/opt/sub2api/sub2api`，只执行一次生产重启。
- 若启动、迁移或在线验收失败，立即恢复部署前 R25 二进制；R26 不新增数据库迁移。

## 验收标准

- `go test ./...`、前端全量测试、TypeScript、ESLint 和生产构建全部通过。
- `sub2api.service` 为 `active/running`，部署后 `NRestarts=0`。
- `/health`、游戏页面和游戏 API 正常。
- 实际试玩产生的 `game_slot_spins.paytable_version` 为 `slot-paytable-v4`。
- `winning_lines` 不得引用 `BONUS` 坐标，每条普通中奖倍率必须来自 V4 奖表。

## 部署记录

- 生产部署时间：`2026-07-29 08:05:38 UTC`（北京时间 `2026-07-29 16:05:38`）。
- 在线版本：`0.1.165-r26`，构建标识：`custom-r26`。
- 在线二进制 SHA-256：`73e91b7e26f8321c0ca409a4cdf6c6b0ed29129a13413a29b09561378184adcb`。
- 部署前备份目录：`/opt/sub2api/backups/r26-deploy-20260729T080448Z`。
- R25 二进制备份 SHA-256：`25843d5b0a1a86a4227da18399be8b6efe9ef1bd70b0348cb2b6936df1c43b7f`。
- PostgreSQL 备份 SHA-256：`e30e88eec77ac712c92b978288c14bb94f9f5be35e484bb9670e55c4fc6aeef2`，`pg_restore --list` 校验通过。
- VPS 完整 `go test ./...`、前端全量 Vitest、TypeScript、ESLint 和生产构建均通过。
- 部署后服务为 `active/running`，`NRestarts=0`；本地健康检查、游戏页面及公网游戏页面均返回 HTTP 200。
- 已在登录态 Chrome 真实试玩，并对当时全部 34 条 V4 转动进行数据库审计：`BONUS` 中奖坐标引用为 0，非法倍率为 0。
