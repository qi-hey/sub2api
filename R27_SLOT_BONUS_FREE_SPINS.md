# Sub2API 0.1.165-r27 老虎机下注与奖励局

R27 保留 R11-R26 的全部二开功能，只调整金运老虎机的服务端计奖、奖励局和对应前端。

## 下注与普通奖表

- 支持下注：`1 / 2 / 5 / 10 / 20 / 50 / 100`。
- 下注 10 时，每个中奖组合直接按下表得分；其他下注按比例结算，所有组合汇总后只取整一次。
- 其他图标按原奖表倍率统一乘 5，换算为下注 10 时的直接得分。

| 图标 | 3 轴 | 4 轴 | 5 轴 |
| --- | ---: | ---: | ---: |
| 金色 7 | 80 | 300 | 1200 |
| 龙 | 40 | 160 | 500 |
| 元宝 | 10 | 60 | 240 |
| 玉牌 | 5 | 40 | 160 |
| 金铃 | 5 | 10 | 15 |

前端显示实际得分和本局明细，例如：`元宝五轴 x1 + 玉牌四轴 x2 = 320 分`，不再要求用户换算旧奖倍。

## BONUS 奖励局

- 每次付费或普通免费转动独立使用 `109 / 8000 = 1.3625%` 的服务端触发门。
- 触发后盘面真实出现 3、4 或 5 个 BONUS，最终奖励分别再乘 `x1 / x2 / x4`。
- BONUS 不作万能卡，也不参与普通 243 Ways 计奖。
- 用户可选择 `8 轮 x5`、`12 轮 x3` 或 `20 轮 x2`，三种方案的基础局总量均为 40。
- 选择前，服务端已经用加密随机源生成并持久化该奖励局的全部结果；重试、刷新或并发领取不能改变结果。
- 删除“一人每天只能触发一次”的唯一约束；保留“一次转动只能创建一个奖励局”和 Token 唯一约束。

## 免费转动与幂等

- 钱包还有普通免费转动时，前端不发送 `bet_credits`，由服务端消费免费次数。
- 付费转动才发送用户当前选择的下注。
- 不确定网络错误使用同一个幂等键和原请求重试，避免重复扣分或重复派奖。
- 多个未领取奖励局按创建时间 FIFO 恢复。

## 数学核验

- 五轴均为 60 个停格，共枚举 `60^5 = 777,600,000` 个基础组合。
- V6 基础盘面 RTP：`112.0419%`。
- BONUS 奖励局和 SCATTER 免费转动还会继续增加返奖；该奖表按指定分值实现，不再沿用旧版约 `92.42%` 的综合 RTP 目标。
- 返奖率仅约束长期数学期望，单次或短期结果仍由服务端随机数决定。

## 数据库迁移

迁移 `196_slot_bonus_free_spins.sql`：

- 删除 `uq_game_slot_bonus_user_date`。
- 保留 `uq_game_slot_bonus_trigger_spin`。
- 保留 `uq_game_slot_bonus_token_hash`。
- 奖励计划 JSON 扩展为 `slot-bonus-v3`，同时兼容历史宝箱和转盘奖励。

## 版本

- 应用版本：`0.1.165-r27`
- 构建标识：`custom-r27`
- 奖表版本：`slot-paytable-v7`
- 转轴版本：`slot-reels-v4`
