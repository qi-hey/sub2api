# R21 中文游戏积分与老虎机前端任务

## 目标

在当前 R21 独立目录中完成前端实现。只修改 `frontend/`，不要修改后端、R20、生产环境或部署文件。

必须先阅读 `R21_CLAUDE_TASK.md`、现有 R20 游戏中心、R21 的 `backend/internal/handler/dto/game_wallet.go`、`backend/internal/handler/game_wallet_handler.go` 和管理员设置 DTO，严格按现有接口接入，不得在浏览器端计算或伪造可兑换积分、中奖结果、赔率、免费旋转或兑换码。

## 中文要求

- 所有用户可见文字必须是简体中文，包括标题、按钮、标签、空状态、加载状态、错误提示、无障碍标签、提示框和老虎机图案文字。
- 不允许残留 `JACKPOT`、`SPIN`、`WILD`、`SCATTER`、`Lucky` 等英文界面文字；可分别显示为“头奖”“旋转”“百搭”“福星”“金运”。
- API 字段、TypeScript 标识符、测试名称和内部代码可保留英文。
- 优先维护 `frontend/src/i18n/locales/zh/games.ts` 和中文管理员设置文案；其他语言可使用中文作为 R21 回退，不能让中文界面因缺少键而显示英文键名。

## 用户端

1. 删除 R20 余额与积分双向兑换 UI 和 `/game-wallet/exchange` 前端调用。
2. 游戏中心顶部显示：游戏积分、API 余额、剩余免费旋转、今日签到状态。
3. 提供“每日签到”按钮，成功后展示本次获得积分和最新余额；重复点击和网络重试必须使用稳定的幂等键并防止重复提交。
4. 新增“奖励兑换”区域：中文奖励名、所需积分、API 额度、今日剩余、有效期、兑换按钮。成功后清晰展示兑换码并提供复制按钮。
5. 新增积分明细：签到、下注、中奖、奖励兑换四类中文标签，支持继续加载。
6. 功能关闭或经济参数未配置时，显示明确的中文维护状态；其他六个本地游戏仍可玩，但不能增加可兑换积分。

## 5x3 澳门风格老虎机

- 将 `LuckyScene` 改造成黑、正红、金色的 5 列 3 行、20 线老虎机，风格精致但不得使用真实赌场品牌或版权素材。
- 使用现有 Phaser 和 Vue 架构。动画可以先滚动，但最终必须逐格停在服务端 `slot/spin` 返回的 `grid`；中奖线、奖金、积分和免费旋转只显示服务端数据。
- 图案使用原创、代码生成或项目已有资源表现：金色 7、龙、元宝、玉牌、金铃、百搭、福星。不要依赖外部 CDN。
- 桌面支持鼠标、空格键旋转；手机按钮最小高度 52px。旋转中禁用重复操作，页面布局不得因动态数字跳动。
- 保留 Web Audio 首次交互解锁、静音记忆；补齐转轴、停轮、普通中奖、大奖、未中奖、签到、兑换成功音效。音效应轻量且不能卡主线程。
- 尊重 `prefers-reduced-motion`，减少动画但不能跳过服务端请求。
- 不显示客户端伪造的本地筹码，不把其他本地游戏分数混入服务端积分。

## 管理员设置

将旧“游戏钱包兑换”设置卡替换为“游戏积分与奖励”中文卡片，接入：

- `game_loyalty_enabled`
- `game_loyalty_checkin_credits`
- `game_loyalty_slot_bet_credits`
- `game_loyalty_daily_reward_limit`
- `game_loyalty_reward_catalog`

奖励目录使用可编辑的结构化表单，不要求管理员手写 JSON；每项支持中文名称、积分成本、API 额度、每日库存、有效天数、启用状态、增删。保存前由前端生成后端要求的精确字符串 JSON。功能开启前必须校验签到积分和每次下注大于 0。

## 代码与测试

- 更新 `types/gameWallet.ts`、`api/gameWallet.ts` 及对应测试，所有积分使用字符串接收后再安全显示，不使用浮点数处理积分。
- 更新游戏中心、游戏页、老虎机场景、音效、中文 i18n 和管理员设置。
- 新增或更新测试，至少覆盖：接口路径与幂等头、签到防重复、奖励兑换与复制、服务端格子落位、旋转防重入、键盘操作、功能关闭、移动控制尺寸、静音记忆和减少动态效果。
- 运行格式检查范围内的 Vitest、`pnpm typecheck` 和 `pnpm build`；修复自己引入的问题。
- 完成后报告修改文件、测试结果和剩余问题，不要部署。

